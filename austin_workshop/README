0. Update Service profile variable value with respective variable name according to setup in function.sh.
1. Update keystone_identity_v3 file with right credentials and IP.
2. Update below line in  run.sh file  with os controller ip (ex:10.102.1.40 is a os controller ip) 
   echo -e "export OS_AUTH_URL=http://10.102.1.40:5000/v3/">>$PWD/keystone_directory/keystone-mem$i
3. Precreate resources 
   bash run.sh 
   run.sh will pre-create 3 tier resources in sequntial order as per the interactive user inputs below
   a) Enter '1'  
   b) Enter total number of tenants
   c) 'create' for create operation which will pre-launch service chains (firewall and loadbalancer VMs,PRSs, PTGS etc).

4. Update resources: This operation is parallel for all tenants 
   bash run.sh 
   a) Enter total number of tenants
   b) Enter total number of tenants
   c) 'update' for updating prs which will launch fw service chain between App and DB groups

Note: Update "dynamic" variable value in function.sh for dynamic service chain 
(default:fw-lb. Modify the value to fw "firewall to be inserted between App and DB groups" or fw-lb "fw-lb") 


5. Delete resources: This operation is parallel for all tenants 
   bash run.sh 
   a) Enter total number of tenants
   b) Enter total number of tenants
   c) 'delete' for deleting all resources from all tenants

   
6. bash user-delete.sh for deleting tenant and user after deleting all gbp resources from all existing mem tenant.


Notes:
User credentil for each tenant :

user:mem<i> where <i> is the tenant number which got created. Ex : mem1,mem2, mem3 ...... memn for n tenants

Password is same as user name for each  tenant.

Domain : default (For all tenants)


For launching vyos fw update variable "fw_prof" with vyos_fw profile name in function.sh

Example :
bash run.sh

Enter number of tenant sets which will be used for resource creation in sequential order
1
Enter total number of tenants
50
Enter create or delete or update (update operation update prs which will launch fw+lb chain)
create

"Now tenant and user creation will strart"

